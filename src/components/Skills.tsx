
const aboutStyle = {
    transition: 'all 2s',
    padding: '40vh 10px 0vh 10px',
    maxWidth: '600px'
}

type Props = {
    windowWidth: number
}

export default function Skills({ windowWidth }: Props) {
    return (
        <section style={aboutStyle}>
            TEST TEXT
        </section>
    );
  }